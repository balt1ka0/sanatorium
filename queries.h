constexpr char create_guest_table[] {"CREATE TABLE guest (passport_number INT PRIMARY KEY, "
                                     "last_name TEXT NOT NULL, "
                                     "first_name TEXT NOT NULL, "
                                     "patronymic TEXT);"};